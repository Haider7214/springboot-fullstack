apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-deployment
  labels:
    app: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crm
  template:
    metadata:
      labels:
        app: crm
    spec:
      containers:
        - name: crm-container
          image: haidertelusko/crm-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8484
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://crm-mysql.czwqy82syuaz.ap-south-1.rds.amazonaws.com:3306/mydb"
            - name: SPRING_DATASOURCE_USERNAME
              value: "crmuser"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rds-secret
                  key: password
